# qosnat2 待开发 / 验收清单

> 最后对照代码与 Web UI：**2026-05-22**。P0–P6 主线及批次 A/B/C、中期待办均已落地；下文区分「已交付」「待完善」「远期」。  
> **自动验收**：`scripts/acceptance-auto.sh`（2026-05-21 本机 **26 PASS / 1 FAIL / 2 SKIP**，FAIL 为未配置 `ADMIN_PASS`）→ 详见 [`docs/验收报告-auto.md`](验收报告-auto.md)。  
> **上行 IFB 修复**（2026-05-21）：`ens19 ingress` 仅 `u32+mirred`；**禁止**同口 `BPF direct-action`（会截断 mirred）；分类在 **ifb0 ingress**；遗留 `flower` 用 `tc filter del … flower src_ip CIDR` 迁移。

## 产品范围

| 支持 | 不支持 |
|------|--------|
| WireGuard VPN | **IPsec、OpenVPN**（无 API/UI/状态；勿规划） |
| IPv4 NAT/QoS 为主 | 完整 SD-WAN / BGP / FRR |
| 单宿主机双网卡拓扑 | netns / flowtable / TC policer |

---

## 已交付（控制面 + Web）

### 核心数据面
- [x] nft NAT（SNAT 池、1:1、前缀映射、端口转发、策略路由网段）
- [x] eBPF `profile_lpm` + 动态 HTB/IFB + `fq_codel` / `cake` 叶子
- [x] Per-IP 活跃池、空闲回收、Mark 隔离审计
- [x] 引导向导 → `setup_complete` 后再加载 TC/nft/BPF

### 批次 A（管理面）
- [x] 高级系统优化（CPU/内存 推荐、conntrack 内存模型、sysctl/RPS/txqueuelen）
- [x] 防火墙规则、Aliases、常规设置、API 密钥、审计日志
- [x] **/32 限速**：统一在 QoS 策略页 `POST /shaper/wizard` 或 profile（已移除独立 `shaper/hosts` API 与 VIP 页）

### 批次 B（可观测与网卡）
- [x] Dashboard 流量历史（5s 采样）
- [x] ethtool **ring** + **offload**（GRO/GSO/RX/TX csum）
- [x] 可选 HTTPS（`TLS_CERT` / `TLS_KEY`）
- [x] `scripts/acceptance-check.sh`、`scripts/test-ui-api.sh`、`scripts/acceptance-auto.sh`

### 批次 C / 中期待办（近期已合并交付）
- [x] Dashboard 小组件 **顺序**（↑↓ + localStorage，非拖拽）
- [x] fq **flows / quantum**（`GET/PUT /api/v1/shaper/tc`）
- [x] DHCPv6 池 + RA 字段（dnsmasq 渲染）
- [x] VLAN / 物理网卡 IPv4：**netplan**（`/etc/netplan/99-qosnat2.yaml` + `netplan apply`）
- [x] 多 WAN 链路（Tier/Metric → 同步 `default` 路由）

### Web 菜单（当前）
Dashboard · 接口（4h 流量/实时速率/ethtool）/路由/DHCP/VLAN/多 WAN/RSS · NAT/防火墙规则/Aliases · QoS 策略/活跃池 · WireGuard · eBPF/Mark/conntrack/抓包 · 常规/高级/密钥/审计/OpenAPI

### 近期变更（2026-05-22）
- [x] 移除 **GeoIP** 页面与 `internal/geoip`、nft GeoIP 集成（`/firewall/geoip` 重定向到规则页）
- [x] 移除 **VIP 主机** 独立页与 `shaper/hosts` HTTP API；`/32` 用向导或 profile 管理
- [x] 接口 **link_speed_mbps**（sysfs + ethtool 兜底）、实时速率按线速百分比 + 手动基准（localStorage）
- [x] 多网卡速率采样修复（每口独立 `ifaceSample`）
- [x] API **网卡名校验**（`netif.ValidateIfaceName`，防 `/sys/class/net` 路径穿越）

---

## 待完善（建议优先级）

### P1 — 文档与契约对齐
| 项 | 现状 | 建议 |
|----|------|------|
| OpenAPI | 已去掉 geoip/hosts；**仍缺** vlan、wan-links、shaper/tc、ethtool、`link_speed_mbps` 等 | 补路径与 schema，与实现同步 |
| README / 开发说明 | 2026-05-22 已对齐菜单与 GeoIP/VIP 移除 | 随 API 变更继续维护 |
| API 冒烟 | `test-ui-api.sh` 登录依赖环境，易失败 | 文档化 `QOSNAT_API_KEY`；CI 可选 job |

### P2 — 功能深化（已有骨架）
| 项 | 现状 | 建议 |
|----|------|------|
| 多 WAN **Weight** | UI/状态有字段，**未**参与选路（无 multipath / fwmark 策略） | `ip route nexthop` 或 policy routing + 文档 |
| 防火墙规则 | API 支持 PUT，**Web 仅增删** | 编辑单条、排序 |
| GeoIP（已移除） | 若需国家阻断，在防火墙规则中用 CIDR alias 或自定义 nft | 另开需求再设计 |
| VLAN / 网卡 | 已改 netplan；与 `50-cloud-init.yaml` 同口时会 **覆盖** 该口定义 | PUT 编辑、apply 失败回滚、与 cloud-init 分工文档 |
| DHCPv6 | 基础 dnsmasq 行；无前缀校验/租约展示 | 校验 IPv6 前缀、leases 解析、与 RA-only 模式说明 |
| Dashboard | ↑↓ 排序，非拖拽 | 可选 `vuedraggable` 或保持现状并文档标明 |
| ethtool | 4 项 offload | 按需扩展 TSO/LRO 等；驱动不支持时友好错误 |
| 鉴权与安全 | Cookie + API Key，**无 RBAC** | 角色/只读账号（若需要） |

### P3 — 质量与运维
| 项 | 现状 | 建议 |
|----|------|------|
| §14 实机验收 | `acceptance-auto.sh` 含 TC/IFB mirred/BPF/nft/API/**iperf**/重启；见 [`验收报告-manual-20260522.md`](验收报告-manual-20260522.md) | 24h 长稳、多 WAN failover、**100.64 profile+mirred** 专项 |
| 状态对账 | `acceptance-auto.sh` 已比对 `profile_lpm` 条数与 `state.json` | 扩展托管路由、Apply 后 nft 片段 hash |
| 单元/集成测试 | store、netif（speed/devname）、tuning 有测试 | 扩展 `SyncWanRoutes`、nft Render |
| 部署 | `web/dist` 需本地 build，不入库 | `deploy-qos-nat.sh` 可选 `-BuildWeb` 或文档强调 |

### 远期（见 `docs/UI开发建议.md`）
- 租户 / VXLAN / ASN 级 QoS
- EVPN/SRv6、Anycast POP、MASQUE/H3 等（**非本仓库范围**，仅 UI 建议参考）
- Unbound/DoT、Captive Portal、HAProxy 等 pfSense 类服务（未规划）

---

## 需实机验收（§14）

### 已自动验收（2026-05-21，本机 `ens19`/`ens18`）

- [x] 服务与健康：`qosnatd`、`/api/v1/health`、`setup_complete`
- [x] 数据面存在：nft `qosnat`、ifb0+HTB、TC bpf ingress/egress、pinned `profile_lpm`
- [x] NAT：SNAT 池 + masquerade、非对称回程 drop
- [x] API 冒烟：34 端点 + NAT 写入（`test-ui-api.sh`）
- [x] **profile_lpm** 条数与 `state.json` 一致（2 条：policy `10.0.0.0/8` + profile `10.0.0.0/16`）
- [x] **/32 覆盖**（历史）：曾用 `shaper/hosts`；现改为 wizard/profile（见 manual 验收报告）
- [x] **重启** `qosnatd` 后 `profile_lpm` 仍为 2 条、`health.bpf=true`
- [x] conntrack 占用正常（验收时 128/2097152）
- [x] **iperf3 上行** `100.64.0.254 → 157.15.107.249`（经 LAN 路由）约 **8 Mbit/s**，`acceptance-auto.sh` 自动验（tag `iperf3-pass`）

### 仍须人工 / 专项环境

- [ ] 内网 **10.x** 客户端 iperf 与 policy `10.0.0.0/8` 对账（`100.64.0.0/24` 已验）
- [ ] **/32 profile** 流量速率 iperf 与 `host_exact` 对账（wizard 添加后）
- [ ] **24h** NAT + QoS 联跑无 conntrack 打满、无 map 泄漏
- [ ] **100.64.0.0/24** 等网段 mirred + profile 与 iperf 8M 对账（内网直连测速见 manual 报告）
- [ ] 多 WAN 两条 default failover（当前 `wan_links: []`）

辅助命令：

```bash
# 一键自动验收（读取 /etc/qosnat2/env 登录）
sudo bash -c 'set -a; source /etc/qosnat2/env; set +a; export QOSNAT_PASS="$ADMIN_PASS"; /opt/qosnat2/scripts/acceptance-auto.sh'

# 仅基础项
sudo /opt/qosnat2/scripts/acceptance-check.sh

# API 冒烟
set -a; source /etc/qosnat2/env; set +a; export QOSNAT_PASS="$ADMIN_PASS"
/opt/qosnat2/scripts/test-ui-api.sh
```

报告：`docs/验收报告-auto.md`（每次运行覆盖写入）。

---

## HTTPS 启用示例

```bash
openssl req -x509 -newkey rsa:2048 -keyout /etc/qosnat2/tls.key -out /etc/qosnat2/tls.crt -days 365 -nodes -subj /CN=qosnat2

# /etc/qosnat2/env 增加：
TLS_CERT=/etc/qosnat2/tls.crt
TLS_KEY=/etc/qosnat2/tls.key

systemctl restart qosnatd
```

---

## 已知限制（维护时勿忘）

- 鉴权：HTTP(S) Session Cookie + `X-API-Key`，无 RBAC / MFA
- Aliases：仅 `ipv4_addr` interval
- 防火墙规则模型简化；复杂策略编辑 `/etc/qosnat2/nftables-qosnat.nft` 后勿被 `Apply` 覆盖（当前每次 Apply 会重写该文件）
- 多 WAN **Weight** 字段当前为占位，不参与内核选路
- 内网 **100.64.0.0/10** 若未建 profile，**不会**命中 ingress mirred（当前仅 policy/profile CIDR 列表）
- VPN 仅 WireGuard；不支持 IPsec / OpenVPN
