# 待发版更新说明

> 发布前请编辑本文件：按 **新增 / 优化 / 修复 / 删除 / 其他** 梳理变动；CI 将据此生成 GitHub Release 说明并归档。
> 完成后与功能代码一并提交；发布成功后本文件会自动重置为模板。

## 概要

独立「出站 / 策略路由」页面：按 WanLink 分标签管理策略路由，新建时自动选用最优 WAN 与 SNAT 默认值。

## 新增

- 网络菜单新增「出站 / 策略路由」页（`/network/egress-policies`），按 WanLink 标签筛选与管理出站策略（对齐防火墙按接口分栏交互）
- 新建策略自动预填：当前 WAN 标签 / 主 WAN、policy-only 隧道默认「仅路由」、SNAT 自动、优先级 100
- 系统托管策略（`auto-iface-pr-*` / `auto-fw-gw-*`）只读展示，避免误改

## 优化

- 多 WAN 页去掉出站策略 Tab，改为引导至独立策略路由页；WARP / 代理文案同步指向新入口

## 修复

- （无）

## 删除

- （无）

## 其他

- （无）
