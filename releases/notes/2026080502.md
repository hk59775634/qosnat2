# qosnat2 2026080502


## 概要

修复删除/修改出站策略后内核 `ip rule` 残留的问题。

## 新增

- （无）

## 优化

- （无）

## 修复

- 删除或修改出站策略时，先按旧策略调用 `DeletePolicy` 清理已安装的 `ip rule`，再 `Apply` 剩余策略（此前仅 Apply，看不到已删除项导致规则残留）
- 接口「策略路由」重建时同样先清理 `auto-iface-pr-*` 对应的内核 `ip rule`，避免关闭/改 IP 后残留

## 删除

- （无）

## 其他

- （无）
